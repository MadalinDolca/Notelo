<?xml version="1.0" encoding="utf-8"?>

<!-- Bottom Navigation menu buttons having the same IDs as @navigation/main_fragments -->
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:id="@+id/contentFragment"
        android:enabled="true"
        android:icon="@drawable/ic_collection"
        android:title="@string/content" />

    <item
        android:id="@+id/addFragment"
        android:enabled="true"
        android:icon="@drawable/ic_add_circle"
        android:title="@string/add" />

    <item
        android:id="@+id/discoverFragment"
        android:enabled="true"
        android:icon="@drawable/ic_explore"
        android:title="@string/discover" />
</menu>